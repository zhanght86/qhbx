#通用配置信息
corp.name=ruaho,inc

#工程名称及前缀
def.name=aeon
def.prefix=aeon

#目录定义
root.dir=..
dist.dir=dist
release.dir=../../build

db.dir=${root.dir}/db
web.dir=${root.dir}/pro
src.dir=${root.dir}/src
web-info.dir=${web.dir}/WEB-INF
web-info-doc.dir=${web-info.dir}/doc
lib.dir=${web-info.dir}/lib
classes.dir=${web-info.dir}/classes
jar.name=rh-${def.name}.jar
war.name=rh-${def.name}.war
zip.jsp.name=jsp-${def.name}.zip
zip.package.name=rh-${def.name}.zip


#临时文件定义
manifest.mf=${dist.dir}/MANIFEST.MF
compile.debug=on